/* here we need check data, before application start work. Validate absolutely all fields. */
import fs from 'fs'

// fs.readdirSync('./data').forEach(file => {
//   // console.log(file);
// })

const getValidNotes = () => {

}

export { getValidNotes }
